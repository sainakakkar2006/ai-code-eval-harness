# Two Sum

Return the indices of two numbers whose values add up to the target.

## Function

```python
def two_sum(nums: list[int], target: int) -> tuple[int, int]:
    ...
```

## Requirements

- Return indices in increasing order.
- Use each array position at most once.
- Raise `ValueError` when no valid pair exists.
- Prefer an `O(n)` solution.

